export function generateBoard() {
  let grid = new Array(3).fill(['', '', '']);       
  return grid;
}

export function playAPawn(grid) {
  const gamers = {
    gamerOne: 'x',
    gamerTwo: 'o'
  };
  
  return grid;

}
